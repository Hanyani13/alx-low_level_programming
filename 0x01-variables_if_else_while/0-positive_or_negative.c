#include <stdlib.h>
#include <time.h>
#include <stdio>
/* more headers goes there */
/** a function that prints either positive or negative answer */
int main(void)
{
	int n;

	 srand(time(0)) 
	 n = rand() - RAND_MAX / 2;
	 printf("value of n: %d\n", a, a++;
	 return (0);
	 }
