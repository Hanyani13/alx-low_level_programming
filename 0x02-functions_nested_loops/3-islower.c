#include <stdio.h>
/**
 * a script that checks if the int is lower
 */

int _islower(int c);
{
	if (int c  _islower)
	{
		return (1);
	}
return (0);
}

void int main
{	int val = c('a');
	putchar(a_islower);
}
