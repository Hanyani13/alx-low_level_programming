#include "main.h"

/**
 * main - check the code
 *
 * Return: Always 0.
 */

int print_sign(int n);
if (n > 0);
{
	putchar('+');
	putchar('\n')
	return (1);
}
void print_sign()

	int n;
	if (n = 0);
{
	return(0)
		else
			return(-1)
}


