#include <stdio>

/** a function that computes the absolute value of an integer
 */

void findAbsolute(int N)
{
	if (N < 0)
	{
		N = (-1) * N;
	}

	N = (-1) * N;
}

cout << ' ' << N;
}

int main(void)
{
	int N = -12;
	findAbsolute(N);
	return 0;
}


