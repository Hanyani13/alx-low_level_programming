#include <stdio.h>
#inclide "0-main.c"
/**
 * main - check the code.
 *
 * Return: Always 0.
 */

int _isupper(int c)
{
  int c = 'H';
    if (c == _isupper)
      {
	return (1);
      }
    else
      {
	return (0);
      }
}
