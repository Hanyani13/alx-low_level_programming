#include <stdio.h>
include "main.h"

/**
 * main - check the code
 *
 * Return: Always 0.
 */

int main(void)
{
  int n;

  while (n = 0, n <= 9, n++)
    {
      if (n == 2 || n == 4)
	putcher('n');
      ++n;
    }
  putchar('\n');

    return (0);
}
